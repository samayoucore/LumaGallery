# LumaLocal

Static GitHub Pages build of the Luma Gallery WordPress theme.

## Commands

```bash
npm install
npm run build
npm start
```

The production-ready site is generated into `static/`.
The local dev server opens at `http://localhost:8095/`.
